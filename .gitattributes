# Auto-detect text files and normalize to LF in the repo.
# Working tree gets LF on Linux/Mac; on Windows with core.autocrlf=true
# it gets CRLF on checkout. Binary files are left untouched.
* text=auto
