# TODO

- 1. [x] Rip out the TTS code - tts is not good enough yet and just destroys immersion. pointless feature: remove it.

- 2. [x] Refactor the chat screen UI - the prompt input should be rendered at the bottom of the message history, not the bottom panel. This is because vertical space is extremely precious in an app where the user reads a continuous feed of content. Having it at the end of the message history makes sense because you typically respond to the latest dialogue/events anyway. We shouldn't remove the bottom panel - we will use this space to display game state-related elements instead, which are currently shoved awkwardly into horizonal menu bars, fighting for space with other UI elements.

- 3. [ ] Add a general Openai v1 agentic loop so we can have more than just anthropic as a provider. Must be compatible with MCP so our mcp implementation can be used with it. We should later add code to expose our MCP in multiple ways since some providers can be configured to use an mcp in multiple ways - such as mcp over http, stdio (usually standard), etc.

- 4. [ ] Add a feature to let the AI generate images by connecting a third-party image generation service like ComfyUI or `/Haoming02/sd-webui-forge-classic/tree/neo` (the most recent continuation of the classic AUTOMATIC1111 webui that people use now ever since lllyasviel stopped maintaining stable-diffusion-webui-forge). Conditionally expose the tools through mcp if the feature is enabled and reachable. Needs its own reusable abstraction layer so not just the mcp can use it, but also be integrated into other parts of the UI if enabled (example: making an expressions generator in the actor editing UI)

- 5. [ ] Relocate data directories to the user's home folder, inside a `.freedungeon` directory that we create on startup.

- 6. [ ] Rename the "Templates" terminology to "Scenario", and scope actors & notes definitions to it instead of having a single global view of actors; keep using the chat db model as the type for scenarios so that importing custom scenarios and sharing them is as simple as importing a chat that's been exported as JSON (needs a json hydration layer). Why do I want this? Because... most of the time, you're only interested in seeing actors that belong to one world instead of all of them at once. You should still be able to create a blank chat and import actors from multiple scenarios (so the user can do silly things like putting sonic and mario in the same room). Our current model is easily modifiable to make this work, and it eliminates the need for the `group` column (user-authored category that causes resource lists that render actors to group them by their `group` string, separated by little header rows that show the string's value) - the listing logic should stay the same but have the headers render the scenario's name instead (name of the chat). If we implement this - it'll need to be documented in the CLAUDE.md file's list of concepts. If a scenario is deleted, we need a strategy for how its actors and notes should be treated, because some chats may still depend on some or all of them. Maybe we'll show the user warnings that say "hey, if you delete this scenario, these actors will be deleted too! please select the ones you wish to keep", then the same warning but with the notes, then a confirmation showing the number of chats that will break as a result of this - OR - we mark actors/notes/scenarios as deleted so they dont show up in searches but don't break existing chats? it'd let users recover deleted definitions too as a bonus feature (the recycling bin screen should be accessed from a button in the home screen dashboard)

- 7. [ ] Let the roleplaying agent decide the absolute width/height of a `webview` container so that it can render content that actually fits in the chat instead of showing a thin, overflow-y scroll container. The max size of an iframe should also be exposed as a getter tool and when they call it, the server should use the client-side size of the message history container to derive the max size - this means that the server needs to know the size of the client's device (multiple devices can render the same chat in real time so there needs to be an intelligent strategy for caching this - probably storing the socket id of the socket where the prompt came from and using it to ask for that client's device viewport size - actually, with this strategy, we dont have to poll or cache the size itself - it's better just have the tool call ask the client about its viewport dimensions when the agent calls the tool directly, especially since it'll be as up-to-date as possible should the user have resized their window (there must be a minimum to clamp to for both width and height in case the user minimizes or makes their browser window incredibly tiny, like 350x350 or something)